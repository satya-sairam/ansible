 - name: sample play 1
   hosts: all
   vars:
    URL: google.com
   tasks:
    - name: print play level variables
      ansible.builtin.debug:
        msg: "{{URL}}"

 - name: sample play 2
   hosts: all
   tasks:
    - name: print play level variables
      ansible.builtin.debug:
        msg: "{{URL}}"
      vars:
         URL: task.google.com


